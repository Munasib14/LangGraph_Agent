-- Add TRY/CATCH and avoid SELECT *
Refactor the SQL below by:
- Replacing SELECT * with meaningful column names
- Wrapping the core logic inside a TRY/CATCH block
- Adding comments to guide future developers

{{ input_sql }}